/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['www.unsplash.com','www.pinterest.com'], // İzin verilen domainler
    },
  };
  
  export default nextConfig;